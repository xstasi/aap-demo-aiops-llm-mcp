---
# --- AAP credentials ---
controller_credentials:
  # SSH credential for connecting to managed web server hosts
  - name: "{{ machine_host_credential_name }}"
    organization: "{{ demo_organization_name }}"
    credential_type: Machine
    inputs:
      username: "{{ machine_ssh_user }}"
      ssh_key_data: "{{ machine_ssh_key }}"
    state: present

eda_credentials:
  # AAP controller credential used by EDA rulebook activations to
  # trigger job templates via run_job_template. Required in AAP 2.5+.
  - name: "{{ demo_controller_credential_name }}"
    organization: "{{ demo_organization_name }}"
    credential_type: "Red Hat Ansible Automation Platform"
    description: >-
      Controller credential for EDA to launch job templates.
    inputs:
      host: "https://{{ aap_hostname }}/api/controller/"
      username: "{{ aap_username }}"
      password: "{{ aap_password }}"
      verify_ssl: "{{ aap_validate_certs }}"
